/**
 * Game manager, stores entities, handles events, draws game
 */
var gameobject = {
    x: 0,
    y: 0,
    
    init: function() {
        _.bindAll(this);
    },
    
    draw : function(delta) {
        
    }
    
}
